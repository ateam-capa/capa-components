import {
  Meta,
  Story,
  Props,
  Description,
  Preview
} from "@storybook/addon-docs/blocks";
import { Image } from "../../utils/lazyLoading";

<Meta title="Utils|lazyLoading" />

# lazyLoading

### Image(props: lazyImageProps)

- image lazy loading 해주는 component
- lazyImageProps
  - @param {string} className
  - @param {string} src
  - @param {string} alt(optional)
- @returns {React.ReactNode} component

### 예제코드

```
  <Image className="클래스 이름" src='이미지 경로' alt='이미지 설명' />
```
